
    # class DrawingCanvas(renpy.Displayable):
    #     # Добавляем параметр bg_image для текстуры заметки
    #     def __init__(self, bg_image, width, height, line_color="#2b1a10", **kwargs):
    #         super(DrawingCanvas, self).__init__(**kwargs)
    #         self.width = width
    #         self.height = height
    #         self.line_color = renpy.color.Color(line_color)
    #         self.lines = [] 
    #         self.drawing = False
            
    #         # Загружаем картинку фона средствами Ren'Py и масштабируем её под размер холста
    #         self.bg_displayable = renpy.displayable(bg_image)

    #     def render(self, width, height, st, at):
    #         # 1. Создаем рендер под размеры холста
    #         render = renpy.Render(self.width, self.height)
            
    #         # 2. Отрисовываем картинку заметки в качестве фона холста
    #         bg_render = renpy.render(self.bg_displayable, self.width, self.height, st, at)
    #         render.blit(bg_render, (0, 0))
            
    #         # 3. Получаем поверхность pygame поверх нашей картинки
    #         canvas = render.canvas()
    #         surface = canvas.get_surface()
            
    #         # 4. Рисуем линии кисти прямо по картинке
    #         for line in self.lines:
    #             if len(line) > 1:
    #                 pygame.draw.lines(surface, self.line_color, False, line, 15)
            
    #         return render

    #     def event(self, ev, x, y, st):
    #         # Проверяем, что курсор находится строго в границах картинки заметки
    #         if 0 <= x < self.width and 0 <= y < self.height:
                
    #             # Кликнули левой кнопкой мыши — начали новую линию
    #             if ev.type == pygame.MOUSEBUTTONDOWN and ev.button == 1:
    #                 self.drawing = True
    #                 self.lines.append([(x, y)])
    #                 renpy.redraw(self, 0)
    #                 return
                
    #             # Ведем мышкой с зажатой кнопкой — добавляем точки в линию
    #             elif ev.type == pygame.MOUSEMOTION and self.drawing:
    #                 if self.lines:
    #                     self.lines[-1].append((x, y))
    #                     renpy.redraw(self, 0)
                        
    #             # Отпустили мышку — закончили линию
    #             elif ev.type == pygame.MOUSEBUTTONUP and ev.button == 1:
    #                 self.drawing = False
    #         else:
    #             # Если курсор вышел за пределы холста во время рисования — прерываем линию
    #             if self.drawing:
    #                 self.drawing = False


    #     def clear(self):
    #         self.lines = []
    #         renpy.redraw(self, 0)

# screen drawing_screen():
#     # Добавляем наш класс на экран
#     add DrawingBoard()

#     # Кнопка для выхода, чтобы не зависнуть на экране бесконечно
#     textbutton "Завершить рисование":
#         align (0.9, 0.05)
#         action Return()